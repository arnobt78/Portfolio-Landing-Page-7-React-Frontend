import NavigationDots from './NavigationDots';
import SocialMedia from './SocialMedia';
import Navbar from './Navbar/Navbar';

// Shared UI: navbar (hash nav + mobile menu), social icons, section dots.
export {
  NavigationDots,
  SocialMedia,
  Navbar,
};
